import React from "react";
import { View, Text } from "react-native";
import { ActivityItem } from "../../Interfaces";

const ActivityItem: React.FC<ActivityItem> = ({ level, name }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default ActivityItem;
